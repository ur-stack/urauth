# FastAPIAuth

The central orchestrator that wires together authentication, authorization, token management, and routing.

::: fastapi_auth.FastAPIAuth
    options:
      members:
        - __init__
        - current_user
        - requires
        - password_auth_router
        - register_oauth_provider
        - oauth_router
        - set_rbac
        - set_permissions
        - lifespan
